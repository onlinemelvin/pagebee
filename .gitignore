# dependencies
node_modules/

# next.js
.next/
out/
build/

# env & secrets — never commit these (the pre-commit hook also blocks them)
.env
.env.*
!.env.example

# prisma generated client (if output is set inside the repo)
# /generated

# claude code — personal/local settings (keep machine-specific perms out of the repo)
.claude/settings.local.json

# test coverage output
coverage/

# misc
.DS_Store
*.log
.vercel

# Playwright MCP session artifacts
.playwright-mcp/
.env.local

# Claude Code agent worktree artifacts (transient)
.claude/worktrees/
.clone/
